---
deployment:
  tasks:
    - export DEPLOYPATH=/home/digistas/public_html/smartmail.store/wp-content/plugins/smartmail-software-store
    - echo "Deploying to ${DEPLOYPATH}"
    - /bin/cp -R . ${DEPLOYPATH}